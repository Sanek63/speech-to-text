torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0
# openai-whisper больше не используется для самой ASR (см. pipeline/asr.py) -- только чтобы
# скачать .pt-чекпоинт с Azure CDN и локально сконвертировать его в CTranslate2, без HF
# (pipeline/ct2_convert.py, pipeline/_openai_to_hf.py)
openai-whisper
faster-whisper==1.2.1
ctranslate2==4.8.1
transformers==5.14.1
nemo_toolkit[asr]
omegaconf
pymupdf
psycopg[binary]
redis
